<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link rel="stylesheet" href="/search.css">
    
</head>
<body>
    <div class="container">
        <h1><a href="/posts">LinkedIn</a></h1>  
    
    <h1>Search Results</h1>
    <% if (posts.length > 0) { %>
        <% for (let post of posts) { %>
            <div class="post">
                <h3><%= post.name %></h3>
                <% if (post.image) { %>
                    <img src="/<%= post.image %>" alt="Post Image" height="100px" width="100px">
                <% } %>
                <p><%= post.content %></p>
            </div>
        <% } %>
    <% } else { %>
        <p>No posts found</p>
    <% } %>
</div>
</body>
</html>
